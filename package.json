{
  "name": "blockchain-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@reduxjs/toolkit": "^1.5.1",
    "@truffle/hdwallet-provider": "^1.4.0",
    "randomcolor": "^0.6.2",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-redux": "^7.2.4",
    "react-scripts": "4.0.3",
    "react-svg": "^13.0.5",
    "rsuite": "^4.10.1",
    "web3": "^1.3.5"
  },
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "scripts": {
    "local-redeploy-contract": "rimraf src/contracts_abi && truffle deploy --reset --network development",
    "redeploy-contract": "rimraf src/contracts_abi && truffle deploy --reset --network kovan",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
